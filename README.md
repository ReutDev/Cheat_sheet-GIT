# Ўпаргалка: Ќачало работы с Git

## »нициализаци€ репозитори€

- `git init` Ч инициализаци€ репозитори€.

## —инхронизаци€ локального и удалЄнного репозиториев

- `git remote add origin <url>` Ч прив€зка локального репозитори€ к удалЄнному.
- `git remote -v` Ч проверка св€зи репозиториев.
- `git push -u origin main` Ч первоначальна€ загрузка коммитов в удалЄнный репозиторий.

### ѕримечание:
¬аша ветка может называтьс€ `master`, а не `main`. »змените команду при необходимости.

- `git push` Ч загрузка коммитов в удалЄнный репозиторий после первоначальной настройки.

## ѕодготовка файла к коммиту

- `git add <file>` Ч подготовка файла к коммиту.
- `git add --all` или `git add .` Ч подготовка всех изменЄнных и новых файлов.

## —оздание и публикаци€ коммита

- `git commit -m " омментарий к коммиту."` Ч создание коммита с комментарием.
- `git push` Ч отправка изменений в удалЄнный репозиторий.

## ѕросмотр информации о коммитах

- `git log` Ч просмотр истории коммитов.
- `git log --oneline` Ч краткий список коммитов.

## ѕросмотр состо€ни€ файлов

- `git status` Ч текущее состо€ние репозитори€.

## ƒобавление изменений в последний коммит

- `git commit --amend --no-edit` Ч добавление изменений к последнему коммиту без изменени€ сообщени€.
- `git commit --amend -m "Ќовое сообщение"` Ч изменение сообщени€ последнего коммита.

## Ђќткатї файлов и коммитов

- `git restore --staged <file>` Ч отмена добавлени€ файла в staged.
- `git restore <file>` Ч возврат файла к последней сохранЄнной версии.
- `git reset --hard <commit>` Ч удаление незакоммиченных изменений.

## ѕросмотр изменений

- `git diff` Ч отображение изменений в рабочей зоне (modified files).
- `git diff <commit1> <commit2>` Ч разница между двум€ коммитами.
- `git diff --staged` Ч изменени€, добавленные в staged files.